from novelty_detection.som import SOM
from novelty_detection.preprocessing import minmax_scaler



# data = minmax_scaler(test_x) # normalisation

# model=SOM(max_steps=10000, random_state=40)
# model=model.fit(X,y)